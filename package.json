{
  "name": "functional-programming-intro",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch": "browser-sync start -s --port 8899 --files \"./dist/*.js, *.html\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.18.13"
  }
}
